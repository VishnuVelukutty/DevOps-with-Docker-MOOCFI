Exercise 1.1

1. docker image pull <image> OR docker pull <image-name>
2. docker container run <image-name> OR docker run <image-name>
3. docker image ls OR docker images         ? Optional 
4. docker container stop <container> OR docker stop <container>
5. docker container ls -a OR docker ps -a  

Exercise 1.2

1. docker container stop <container> OR docker stop
2. docker container rm <container> OR docker rm <container>
3. docker image rm <image> OR docker rmi <image>
4. docker ps -a
